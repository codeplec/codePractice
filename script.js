$(document).ready(function() {
    $('div').fadeOut(1000);
    // Target element 'div' will disapper (fadeout) upon document loading
    // My guess is that (1000) represents 1 sec -> 1000 miliseconds 
});
